'use client'
export function DocumentsPage({ refreshKey, onRefresh }: { refreshKey?: number; onRefresh?: () => void }) {
  return <div className="flex items-center justify-center h-64 text-[#5a6a80]"><p>Dokumentumközpont – hamarosan</p></div>
}
