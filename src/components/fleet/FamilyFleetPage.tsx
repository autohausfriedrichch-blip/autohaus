'use client'
export function FamilyFleetPage({ refreshKey, onRefresh }: { refreshKey?: number; onRefresh?: () => void }) {
  return <div className="flex items-center justify-center h-64 text-[#5a6a80]"><p>Family Fleet – hamarosan</p></div>
}
