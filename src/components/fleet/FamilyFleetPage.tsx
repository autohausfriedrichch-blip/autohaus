'use client'
export function FamilyFleetPage({ refreshKey, onRefresh }: { refreshKey?: number; onRefresh?: () => void }) {
  return <div className="flex items-center justify-center h-64 text-[#4a4a4a]"><p>Family Fleet – hamarosan</p></div>
}
